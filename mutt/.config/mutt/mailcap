text/html; setsid -f xdg-open %s ; nametemplate=%s.html
text/html; w3m -o ext_image_viewer=0 -o auto_image=TRUE -o display_image=1 -T text/html %s; nametemplate=%s.html; needsterminal
text/html; w3m -I %{charset} -T text/html -cols 140 -o display_link_number=1 -dump; copiousoutput
# text/html;  w3m -I %{charset} -T text/html -cols 140 -o display_link_number=1 -o display_image=true -o auto_image=true -dump; nametemplate=%s.html; copiousoutput
text/plain; vim %s ;
image/*; kitty icat %s ;
image/*; setsid -f xdg-open %s ;
video/*; setsid mpv --quiet %s &; copiousoutput
audio/*; mpv %s ;
application/pdf; setsid -f xdg-open %s ;
application/pgp-encrypted; gpg -d '%s'; copiousoutput;
application/pgp-keys; gpg --import '%s'; copiousoutput;
